{
    "schedule": {
        "repeat": "{{repeat}}"
    },
    "schema": 1,
    "test": {
        "spec": {
            "dest": "{{destination}}",
            "packet-count": {{packetCount}},
            "schema": 1
        },
        "type": "{{type}}"
    },
    "tools": [
        "{{tool}}"
    ]
}